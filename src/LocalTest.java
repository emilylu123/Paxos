public class LocalTest {
    public static void main(String[] args) {
        String memberID = "M2";
        int port = Integer.parseInt(memberID.substring(1));
        System.out.println(port);
    }
}
